{% extends "base.html.twig" %}

{% block title %}
	{{ category.name }} | Ganache &amp; Cabosse | Rethel
{% endblock %}

{% block body %}
	<!-- intro -->
	<section class="padding-top-header position-relative z-index-1" data-theme="default">
		<div class="container max-width-adaptive-lg padding-top-lg padding-bottom-xl position-relative z-index-2">
			<div class="text-center">
				<p class="text-uppercase text-xs letter-spacing-lg color-contrast-medium">Nos</p>
				<h1 class="text-xxxl text-uppercase">{{ category.name }}</h1>
			</div>
		</div>
	</section>

	<!-- products -->
	<section class="position-relative z-index-1 padding-top-lg padding-bottom-lg">
		<div class="container max-width-adaptive-lg">
			<div class="margin-bottom-md">
				<nav class="breadcrumbs text-sm" aria-label="Breadcrumbs">
					<ol class="flex flex-wrap gap-xxs">
						<li class="breadcrumbs__item">
							<a href="{{ path('homepage') }}" class="color-inherit">Accueil</a>
							<svg class="icon margin-left-xxxs color-contrast-medium" aria-hidden="true" viewbox="0 0 16 16">
								<g stroke-width="1" stroke="currentColor">
									<polyline fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-miterlimit="10" points="6.5,3.5 11,8 6.5,12.5 "></polyline>
								</g>
							</svg>
						</li>
						<li class="breadcrumbs__item" aria-current="page">{{ category.name }}</li>
					</li>
				</ol>
			</nav>
		</div>

		<ul class="grid gap-sm">
			{% for p in category.products %}
				<li class="col6@xs col-4@sm col-3@md">
					<div class="prod-card-v2">
						<a href="{{ path('product_show', {'category_slug': p.category.slug, 'slug': p.slug}) }}" class="prod-card-v2__img-link" aria-label="Description of the link">
							<figure class="media-wrapper media-wrapper--4:5">
								<img src="/uploads/{{ p.mainPicture }}" alt="{{ category.name }} | {{ p.name }} | Ganache &amp; Cabosse">
								{% for image in p.images %}
									<img src="/uploads/{{ image.url }}" alt="{{ category.name }} | {{ p.name }} | Ganache &amp; Cabosse">
								{% endfor %}
							</figure>
						</a>

						<div class="padding-sm text-center">
							<h2 class="text-md font-primary">
								<a href="{{ path('product_show', {'category_slug': p.category.slug, 'slug': p.slug}) }}" class="product-card-v2__title">{{ p.name }}</a>
							</h2>
						</div>
					</div>
				</li>
			{% endfor %}
		</ul>
	</section>
{% endblock %}

{% block footer %}
	{% include '_partials/footer.html.twig' %}
{% endblock %}
